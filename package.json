{
  "name": "optical-adjust",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run build -r",
    "check": "vp check",
    "dev": "vp run website#dev",
    "prepare": "vp config",
    "ready": "vp fmt && vp check && vp test && vp run build -r",
    "test:optics": "cd packages/optics && vp test --coverage",
    "test:render": "cd packages/optics-render && vp test"
  },
  "devDependencies": {
    "@playwright/test": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.33.0"
}
